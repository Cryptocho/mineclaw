# MineClaw 项目计划

## 项目概述

**MineClaw** 是一个革命性的 AI 智能体平台，从传统的单线程、人工切换上下文的 Claw 架构，演进到**多 Agent 并行 + 自动化编排协作**的新一代架构。

### 核心愿景

- **从"单线程"到"并行协作"**：不再是一个 Agent 顺序执行任务，而是多个 specialized Agent 同时工作
- **从"人工切换"到"自动化编排"**：由系统智能地分解任务、分配工作、协调协作，无需人工干预
- **从"单一能力"到"专业分工"**：不同 Agent 具备不同专长（代码专家、文档专家、测试专家等），协同完成复杂任务
- **从"被动执行"到"主动规划"**：系统能自主理解目标、制定计划、监控进度、调整策略

### 为什么需要多 Agent 架构？

传统 Claw 的局限性：
1. **单线程瓶颈**：一个 Agent 同时只能做一件事，效率低下
2. **注意力分散**：同一个 Agent 需要在不同任务间切换，质量下降
3. **能力局限**：单个模型难以在所有领域都达到专家水平
4. **人工依赖**：需要用户不断引导和切换上下文

MineClaw 的突破：
1. **并行执行**：多个 Agent 同时工作，显著提升效率
2. **专业分工**：每个 Agent 专注特定领域，质量更高
3. **智能编排**：系统自动协调，无需人工干预
4. **弹性扩展**：可根据需要动态增加或减少 Agent

## 核心架构设计

### 整体协作模式

MineClaw 采用**混合协作模式**，结合两种核心协作方式：

#### 1. 集群并行模式
多个 Agent 同时工作，各自承担不同子任务，适用于：
- **创意脑洞**：多个 Agent 从不同角度发散思维
- **API 查询与学习**：并行查询文档源、交叉验证
- **大项目代码分工**：约定接口后，每个 Agent 实现一部分
- **多进程测试**：并行执行测试用例，大幅缩短时间

#### 2. 接力转交模式
任务被拆分为多个阶段，每个阶段由专门的 Agent 完成，完成后通过 JSON 工单转交下一 Agent，适用于：
- **持续犯错**：当前 Agent 陷入困境时，换新鲜视角
- **复杂项目分阶段**：每个阶段刷新上下文，避免过长

### 层级化总控架构

```
主总控 (Master Orchestrator) - 最顶层，唯一
    │
    ├── 子总控 1 (Sub-Orchestrator, Level 1)
    │   ├── 可以创建：普通 Agent
    │   ├── 可以创建：更小的子总控 (Level 2，如需要)
    │   └── 可以创建：集群（并行执行多个 Agent）
    │
    ├── 子总控 2 (Sub-Orchestrator, Level 1)
    │   └── ...
    │
    └── 上下文管理 Agent (Context Manager Agent, CMA)
            ↓
       独立监控所有 Agent
```

#### Agent 类型定义

**主总控 (Master Orchestrator)**
- 最顶层的唯一总控
- 负责初始路由决策
- 管理所有子总控的生命周期

**子总控 (Sub-Orchestrator)**
- 可以嵌套，知道自己的嵌套深度 (Level N)
- 可以创建普通 Agent、更小的子总控或集群
- 完成任务后写工单交回给父总控
- 提示词会软性劝阻过深嵌套，但不硬性限制

**普通执行 Agent (Worker Agent)**
- 具体执行任务的无状态执行者
- 可以主动向 CMA 发送求助工单
- 不知道其他 Agent 的存在

**上下文管理 Agent (Context Manager Agent, CMA)**
- 独立监控所有 Agent
- 接收 Agent 求助和处理上下文满载
- 决定回退和转交，但不直接执行

#### 总控 Agent 的特殊能力
- **最强模型配置**：通常使用能力最强的模型
- **创建 Agent 实例**：可以编写提示词动态创建新 Agent
- **编写工作流**：定义任务分解和执行流程
- **计划与评审**：只在每一小步的开始和结束时介入
- **可被重置**：总控犯错也会被上下文管理 Agent 识别并重置

#### 汇报机制
- **分支总控**：处理分支内的反馈
- **主总控**：只在分支结束时介入
- **汇报内容**：只包含完成内容和未解决的问题

### 上下文管理 Agent (Context Manager Agent, CMA)

上下文管理 Agent 是整个工作流的"监军"，独立监控所有 Agent。

#### 触发机制（只有两种情况）

**触发点 1：普通 Agent 主动求助（Worker → CMA）**
- 普通 Agent 发现自己持续犯错 → 生成"求助工单"
- 求助工单直接发送给 CMA（不是主总控！）
- 求助工单内容：
  - 我是谁（Agent ID/角色）
  - 我在做什么任务
  - 我尝试了什么，失败了多少次
  - 相关的错误信息
  - 我建议回退到哪个 Checkpoint（可选）

**触发点 2：上下文满载（唯一的自动触发点）**
- 检测到：当前上下文长度 ≥ 阈值（如 90%）
- 自动触发：CMA 进行清理
- 其他情况不自动触发，因为每次输入的上下文通常很长

#### CMA 干预流程

**情况 A：收到 Agent 的主动求助**
1. CMA 接收求助工单 → 分析情况是否确实严重
2. CMA 决定应该回退到哪个 Checkpoint
3. CMA 触发当前层级的总控进行转交
4. 总控负责实际的回退和创建新 Agent
- （CMA 是"监军"，不是"主帅"，不直接操作执行）

**情况 B：上下文满载**
1. CMA 执行上下文清理（裁剪）
   - 分析哪些可以裁剪（反复失败的尝试、冗余对话等）
   - 执行裁剪（像修改文件一样编辑上下文）
   - 创建 Checkpoint（保存裁剪前的状态）
2. CMA 判断："这个 Agent 是在持续犯错吗？"
   - 是 → 触发回退 + 转交流程（同情况 A）
   - 否 → 清理完毕，让任务继续执行

#### 裁剪策略参考（提示词模板）
- **持续犯错部分**：裁剪掉反复失败的尝试
- **Bug 解决过程**：保留结果，裁剪过程（未来用于经验学习）
- **终端长输出**：裁剪冗余，如测试日志只保留测试成功/失败总结
- **冗余对话**：删除重复、无关的内容

#### 裁剪质量评估
- 裁剪提示词会被记录
- 由评审 Agent 或用户根据任务完成质量评分
- 评分结果将参与未来的提示词自动优化（Phase 7+）

### 工单机制 (Work Order)

接力转交时使用 JSON 格式工单，包含：

```
工单内容规范：
├── 已完成部分详细标注
├── 相关文件的相对路径列表
└── 下一阶段的详细计划
```

**设计原则**：工单应简洁、聚焦，仅包含必要信息，避免成为新的上下文负担。

### 路由模型 (Router Model)

#### 触发时机
1. **任务开始时**：初始判断使用单 Agent 还是多 Agent 模式
2. **上下文裁剪后**：上下文被管理 Agent 裁剪后，重新评估路由策略

#### 路由决策逻辑

**简单任务** → 单总控 Agent 模式（或创建单个普通 Agent）

**复杂任务** → 判断使用哪种子总控模式

---

### 复杂任务的两种执行模式

#### 模式 A：嵌套子总控接力模式
**适用场景**：复杂任务，各步骤之间强依赖顺序，每一步都很复杂

**执行流程**：
```
主总控 → 创建子总控 1 (Level 1)
    ↓
子总控 1 执行（内部可能创建集群并行执行）
    ↓
子总控 1 完成 → 写工单 → 主总控
    ↓
主总控更新计划 → 创建子总控 2 (Level 1)
    ↓
子总控 2 执行 → 写工单 → 主总控
    ↓
... 重复直到所有阶段完成 ...
    ↓
主总控汇总结果
```

**示例**：主总控 → (总控1→集群→总控1写工单→主总控) → (主总控更新计划后创建新总控2→集群)...

---

#### 模式 B：平行子总控集群模式
**适用场景**：复杂任务，但各部分可以分别独立执行

**执行流程**：
```
主总控 → 分解任务为多个独立子任务
    ↓
主总控同时创建：
    ├─ 子总控 1 (Level 1) - 负责子任务 A
    ├─ 子总控 2 (Level 1) - 负责子任务 B
    └─ 子总控 3 (Level 1) - 负责子任务 C
    ↓
所有子总控并行执行（每个内部可能创建自己的集群或 Agent）
    ↓
子总控 1 完成 → 写工单 → 主总控
子总控 2 完成 → 写工单 → 主总控
子总控 3 完成 → 写工单 → 主总控
    ↓
主总控汇总所有结果
```

**示例**：主总控 → (总控1, 总控2... 计划后创建 → Agent/集群 Agents)...

---

**混合模式**：一个项目可以灵活组合使用两种模式
- 集群为每个 Agent 分派任务
- 每个 Agent 自己把任务拆分成 Baby Steps，通过接力转交完成
- 每个集群分支都有独立的分支总控管理

### 工具掩码策略

#### 掩码范围
- **统一处理**：MCP 工具和本地工具统一管理
- **掩码单位**：单个工具（而非整个 MCP 服务器）

#### 工具权限分类
- **MCP 工具**：只有可用/不可用两种状态（权限由 MCP 服务器端控制）
- **本地工具**：支持只读/读写一键切换，也可单个工具精细配置
- **终端工具**：所有 Agent 都可以使用，无限制

#### 分配策略
- **主总控 Agent**：拥有全部权限
- **Worker Agent**：由总控在分配任务时动态配置工具权限
- 确保分配的工具通常足够完成任务
- 避免工具过多导致的分心

### Checkpoint 与会话管理

- **强关联设计**：Checkpoint 与 Session 一一对应
- **生命周期管理**：
  - Session 归档 → 相关 Checkpoint 归档或删除
  - 由 AgentFS 提供完整实现
- **轻量设计**：完全不会成为系统负担

### 转交预热 (Handover Warm-up)

**必须流程**：在正式转交任务前，需要进行预热

- 让新 Agent 先理解工单内容
- 确认关键信息无遗漏
- 准备好后再正式接管

### 线性流水线设计

- **设计原则**：流水线是线性的，不支持Agent间通信
- **消息追踪**：每个 Agent 的回复都有唯一的消息 ID 记录
- **简化协作**：避免复杂的Agent间依赖，让流程更清晰可控

### 独立大屏与全景可视化前端

MineClaw 放弃了与现有 IDE 强绑定的插件形式，而是选择成为一个拥有独立控制台的“AI 自动化工厂”。

#### 为什么选择独立前端？
- **全景工作流可视化**：可以直观展示 Agent 树状层级（谁在指挥谁）、工单 (Work Order) 的流转路径。
- **Checkpoint 交互式管理**：像 Git 树一样查看所有快照，实现时间轴式的代码变更对比 (Diff) 与一键回退。
- **零干预模式 (Autonomous Mode)**：用户只需看大盘监控，出现异常时（如 CMA 告警或 Agent 主动求助）再进行轻量级干预。
- **掌控底层存储**：保留 Rust 后端对 `AgentFS` 和 Checkpoint 的绝对控制权，不再受限于第三方 IDE 的客户端沙箱体系。

#### 集成策略
- **全异步监控 API**：基于 REST + SSE/WebSocket 打造实时状态总线。
- **Flutter 跨平台控制台**：统一的控制中心，负责任务下发、进度追踪和人工审批。

## 已完成阶段

### Phase 1: 基础消息流转 ✅
- [详细文档](./PHASE1.md)
- 项目基础架构
- 配置管理
- 核心数据模型
- LLM 客户端
- Web API 层
- 请求/响应日志

### Phase 2: MCP 集成 ✅
- [详细文档](./PHASE2.md)
- 数据模型和配置扩展
- MCP 协议和基础客户端
- 工具调用功能
- 扩展 LLM 支持工具调用
- 集成工具调用循环
- SSE 流式模式
- API 扩展和管理功能

### Phase 3: 本地工具与 Checkpoint 集成 ✅
- [详细文档](./PHASE3.md)
- ✅ API Key 加密存储
- ✅ 终端工具
- ✅ 文件工具集
- ✅ Checkpoint 集成（与文件工具深度集成）
- ✅ 配置文件动态修改

## 未来计划详细设计

### Phase 4: 多 Agent 基础架构 🔄
- [详细文档](./PHASE4_inprogress.md) (进行中)
- **目标**：建立多 Agent 运行的基础框架，支持层级化总控架构、可嵌套子总控、上下文管理 Agent 的两种触发机制
- **执行原则**：严格按照 Baby Steps™ 方法论执行，分优先级逐步实现，先验证核心概念，再扩展功能

---

#### 🎯 第一优先级：核心基础设施
**目标**：建立 Agent 运行的最小可行基础

##### Phase 4.1: Agent 基础定义（支持嵌套深度）
- Agent 核心数据结构定义
  - AgentId（新type包装Uuid）
  - AgentRole枚举（MasterOrchestrator, SubOrchestrator, Worker, ContextManager）
  - AgentCapability标签系统
  - LlmConfig（模型、温度、top_p、max_tokens等）
  - AgentState枚举（Idle, Busy, Error, RequestingHelp）
  - Agent核心结构（支持nested_depth和parent_orchestrator_id）
  - AgentConfig配置结构
  - AgentTask和AgentTaskResult
  - HelpRequest（Agent求助工单）
- Agent 核心功能
  - Agent创建与配置验证
  - Agent任务执行（async函数，调用LLM和工具）
  - Agent发送求助功能（发送工单给CMA）
  - Agent状态管理与查询

##### Phase 4.2: 总控机制（支持嵌套子总控）
- 总控核心数据结构定义
  - OrchestratorId（新type包装Uuid）
  - OrchestratorRole枚举（Master, Sub）
  - Orchestrator核心结构（支持嵌套，包含自己的Agent）
  - OrchestratorConfig配置结构
  - TaskId和TaskAssignment
  - ParallelTasks（并行任务配置）
  - WorkOrder（工单，作为User Message）
  - CmaNotification（CMA通知）
- 总控核心功能
  - 总控创建（主总控和子总控）
  - Agent管理（创建、列出、查询、移除Agent，支持创建子总控）
  - 任务分配（串行：直接函数调用；并行：tokio task）
  - 任务管理（查询状态、等待完成、取消）
  - 工单生成和处理
  - CMA通知处理（回退和转交）
  - Session与Agent的关联
- 关键设计决策
  - 通信方式：总控和Agent之间通过直接函数调用通信
  - 并发支持：每个Agent任务运行在独立的tokio task中

##### Phase 4.3: Checkpoint 与会话增强 ✅
- Session 生命周期完善 ✅
  - Session 创建、激活、归档、删除
  - Session 与 Checkpoint 强关联
- Checkpoint 管理增强 ✅
  - 跟随 Session 生命周期的 Checkpoint 管理
  - Checkpoint 归档与清理策略
  - AgentFS 集成优化
- **按 Agent 分组的 Checkpoint 列表** ✅
  - 每个 Agent 维护自己的 Checkpoint 列表
  - 支持灵活的回退策略：回退到单个 Checkpoint、回退到 Agent 接手时
  - 移除了 CheckpointType（项目完全自动化，不需要区分 Auto/Manual）
  - Checkpoint 结构体添加 agent_id 字段
  - Session 结构体添加 agent_checkpoints: HashMap&lt;AgentId, Vec&lt;String&gt;&gt; 字段
  - ToolContext 结构体添加 agent_id 字段
  - CheckpointManager 添加 list_checkpoints_for_agent() 方法

---

#### 🏗️ 第二优先级：功能模块
**目标**：在核心基础设施之上，添加关键功能

##### Phase 4.4: 工具掩码基础机制
- 工具权限管理
  - MCP 工具：可用/不可用二元配置
  - 本地工具：只读/读写一键切换 + 单个工具精细配置
  - 终端工具：全开放（特殊处理）
- Agent 工具分配
  - 主总控拥有全部权限
  - Worker Agent 由总控动态配置工具集
  - 工具调用权限检查（调用前验证）
  - 向 LLM 只展示有权限的工具
- AgentTask.tools 字段：保留用于任务级别的工具覆盖（可选）

##### Phase 4.5: 上下文管理 Agent（基础版）
- 基础上下文管理能力
  - 上下文接收与存储
  - 基础的上下文长度监控
  - 裁剪触发条件判断
- 简单的转交判断
  - 基于规则的转交触发
  - 转交时机识别
- 初始提示词模板系统
  - 上下文裁剪策略模板
  - 转交判断策略模板
  - 模板版本管理

---

### Phase 5: 任务编排与路由系统 📋
- [详细文档](./PHASE5_planning.md) (待编写)
- **目标**：实现智能任务分解、路由决策、层级总控和混合协作模式
- **并行开发**：API 已在 Phase 4.6 完成，Flutter 前端可与此阶段并行开发

#### Phase 5.1: 路由模型实现
- 路由决策引擎
  - 任务复杂度评估
  - 初始路由策略选择（单 Agent / 多 Agent）
  - 路由触发时机实现（任务开始 + 上下文裁剪后）
- 场景识别
  - 创意脑洞场景识别
  - API 查询场景识别
  - 大项目分工场景识别
  - 多进程测试场景识别
  - 持续犯错场景识别
  - 分阶段任务场景识别
- 路由再判断机制
- **CMA 上下文阈值自适应**
  - 成功处理几次后稍微放宽阈值（每次 +5%，上限到默认 90%）
  - 频繁失败则收紧阈值，形成反馈闭环
  - 用户仍可手动设置初始阈值
  - 上下文裁剪后的路由重评估
  - 动态调整策略

#### Phase 5.2: 工单机制实现
- 工单数据结构
  - 已完成部分标注格式
  - 相关文件路径列表
  - 下一阶段计划格式
- 工单创建与解析
  - Agent 完成任务后的工单生成
  - 新 Agent 接收工单的解析与理解
  - 工单验证与完整性检查
- 工单传递流程
  - 工单序列化与传递
  - 工单确认机制

#### Phase 5.3: 层级化总控架构
- 总控 Agent 特殊能力
  - 动态创建 Agent 实例（编写提示词 + 配置）
  - 编写工作流定义
  - 计划制定能力
  - 结果评审能力
- 主总控与分支总控
  - 层级关系建立
  - 职责划分（主总控：分支间协调；分支总控：分支内管理）
  - 汇报机制实现（分支总控→主总控）
- 总控介入时机
  - 每步开始时的计划
  - 每步结束时的评审
  - 避免过度干预

#### Phase 5.4: 集群并行模式实现
- 集群任务分解
  - 总控将大任务拆分为并行子任务
  - 接口约定文档生成
  - 依赖关系管理
- Agent 任务分配
  - 根据 Agent 专长匹配任务
  - 任务分配确认
  - 并行执行协调
- 结果汇总与合并
  - 分支总控收集各 Agent 结果
  - 结果一致性检查
  - 合并策略实现

#### Phase 5.5: 接力转交模式实现
- 阶段划分
  - 任务分阶段策略
  - 阶段里程碑定义
  - Baby Steps 拆分支持
- 转交触发机制
  - 持续犯错检测（次数、模式）
  - 阶段完成识别
  - 上下文管理 Agent 的转交判断
- 转交执行流程
  - 工单生成
  - 转交预热（必须）
  - 上下文清理
  - 新 Agent 初始化与接管
- 总控重置机制
  - 总控犯错检测
  - 上下文清理与问题总结
  - 新总控初始化

#### Phase 5.6: 混合模式支持
- 模式组合
  - 集群内的每个 Agent 可以使用接力模式
  - 接力的某个阶段可以触发集群模式
- 嵌套总控
  - 每个集群分支有独立总控
  - 分支总控向主总控汇报
- 混合模式 API 增强
  - 支持灵活的模式组合配置
  - 模式切换机制
  - 为前端提供模式状态查询接口

---

### Phase 6: 高级协作与质量保障 📋
- [详细文档](./PHASE6_planning.md) (待编写)
- **目标**：完善协作细节，提升质量

#### Phase 6.1: 转交预热完善
- 预热流程细化
  - 工单理解检查
  - 关键信息确认
  - 疑问澄清机制
  - 预热成功标准
- 预热超时处理
- **语义复述确认（简化版，新增）**
  - 总控 spawn 新 Agent 后，让新 Agent 先用自己的话复述任务理解
  - 总控确认理解正确后才正式接管
  - 避免工单的"沉默歧义"问题
  - 预热时间限制
  - 超时后的回退策略

#### Phase 6.2: 消息追踪与审计
- 完整消息链路
  - 每个 Agent 回复的唯一消息 ID
  - 消息父子关系记录
  - 完整消息流存储
- 审计日志
  - 所有关键操作记录
  - 转交事件记录
  - 路由决策记录
  - 裁剪操作记录
- API 增强
  - 为前端提供消息追踪查询接口
  - 审计日志查询接口

#### Phase 6.3: 上下文裁剪增强
- 裁剪策略丰富
  - 更多裁剪场景的提示词模板
  - 可配置的裁剪强度
  - 智能裁剪（识别重要信息）
- 裁剪操作记录
  - 裁剪前后对比记录
  - 裁剪策略记录
  - 提示词版本记录
- API 增强
- **关键内容识别（新增）**
  - 裁剪前先标记"绝对不能删"的关键内容
  - 只裁剪未被标记的部分
  - 如果关键内容已超过阈值，主动触发回退而非强行裁剪
- **CMA 决策记录（为 EvoMap 准备）**
  - 记录每次 CMA 干预决策（包括"为什么干预/不干预"）
  - 为 Phase 9 的 EvoMap 集成积累数据
  - 为前端提供裁剪历史查询接口
  - 裁剪策略配置接口

#### Phase 6.4: 裁剪质量评分系统
- 评分机制
  - 评审 Agent 评分接口
  - 用户评分接口
  - 评分维度定义（信息完整性、任务完成质量等）
- 评分数据存储
  - 评分与裁剪策略关联
  - 评分历史记录
- API 增强
  - 为前端提供评分查询接口
  - 评分提交接口

#### Phase 6.5: 提示词模板系统
- 模板管理
  - Agent 提示词模板
  - 裁剪策略模板
  - 路由判断模板
  - 工单模板
- 模板版本控制
  - 版本历史
  - 版本回滚
  - A/B 测试支持
- API 增强
  - 为前端提供模板管理接口
  - 模板版本查询接口

#### Phase 6.6: 线性流水线保障
- 流程控制
  - 确保流水线线性执行
  - 防止意外回传
  - 流程状态机实现
- 异常处理
  - 流水线中断处理
  - 错误恢复策略
  - 人工介入接口
- API 增强
  - 为前端提供流水线状态查询接口
  - 异常处理接口

---

### Phase 7: 完整 API 层 (为前端铺路) 📋
- [详细文档](./PHASE7_planning.md) (待编写)
- **目标**：为 Flutter 前端提供完整、丰富的接口支撑，包含所有必要的管理、监控、配置功能
- **说明**：Phase 6 完成后开始 API 开发，API 完成后 Flutter 前端即可开始

#### Phase 7.1: 核心对话与 Session API
- Session 生命周期管理
  - 创建 Session（支持配置）
  - 查询 Session（详情、列表、过滤）
  - 更新 Session（元数据、配置）
  - 删除 Session
  - 归档/激活 Session
- 消息管理
  - 消息列表（支持分页、过滤、排序）
  - 发送消息（触发 OrchestratorExecutor）
  - 消息详情查询
  - 消息历史导出
- SSE 实时状态总线
  - 推送 Agent 状态变更 (Idle, Busy, Error, RequestingHelp, WaitingForReview)
  - 推送工单流转事件 (WorkOrder Created, Assigned, InProgress, Completed, Failed)
  - 推送 CMA 告警与干预事件 (ContextTrimmed, HandoverRequested, RollbackSuggested)
  - 推送工具事件 (ToolCall, ToolResult, ToolError)
  - 推送系统健康状态事件
  - 推送会话生命周期事件

#### Phase 7.2: 多 Agent 与总控管理 API
- 总控管理
  - 总控模板列表（内置模板 + 自定义模板）
  - 创建总控（支持配置 Agent 阵容、嵌套深度）
  - 查询总控（详情、拓扑结构）
  - 更新总控配置
  - 删除总控
- Agent 管理
  - 动态添加 Agent 到总控
  - Agent 列表（按总控、角色、状态过滤）
  - Agent 详情查询
  - Agent 配置更新
  - 移除 Agent
- 拓扑与可视化
  - 获取总控完整拓扑图（扁平结构）
  - 获取 Agent 父子关系树
  - 获取集群并行任务关系图

#### Phase 7.3: Agent 深度观察 (Inspector) API
- Agent 上下文观察
  - 获取 Agent 当前工作记忆 (Prompt, Memory, Masks)
  - 获取 Agent 历史上下文（分页）
  - 获取上下文裁剪历史
- Agent 执行日志
  - 获取 Agent 的 Thought Chain（思考链）
  - 获取 Agent 的工具调用历史
  - 获取 Agent 的消息收发记录
- Agent 配置管理
  - 获取 Agent 当前配置
  - 更新 Agent 配置（实时生效）
  - 重置 Agent 配置为默认
  - 复制 Agent 配置到其他 Agent

#### Phase 7.4: 任务与工单管理 API
- 任务管理
  - 活跃任务列表
  - 任务详情查询
  - 任务状态查询
  - 取消任务
  - 任务历史记录
- 工单管理
  - 工单列表（按状态、类型、时间过滤）
  - 工单详情查询
  - 创建手动工单
  - 工单审批接口（管理员）
  - 工单历史记录

#### Phase 7.5: Checkpoint 与 AgentFS API
- Checkpoint 管理
  - Checkpoint 列表（按 Session、时间过滤）
  - Checkpoint 详情查询
  - 创建 Checkpoint（手动触发）
  - 恢复到 Checkpoint
  - 删除 Checkpoint
  - Checkpoint 归档
- AgentFS 可视化
  - 获取指定 Checkpoint 的文件树状态
  - 差异对比 (Diff) 接口（提供变更高亮支持）
  - 文件内容查看（只读）
  - 文件历史版本对比

#### Phase 7.6: 系统监控与统计 API
- 系统资源监控
  - CPU、内存、磁盘使用情况
  - 模型 Token 使用统计
  - 成本估算
- **实时成本预估 + 阈值告警（新增）**
  - 从"事后统计"升级到"实时预估"
  - 每个 Agent 开始执行前给出"预估成本范围"
  - 执行过程中实时更新当前累计成本
  - 当总预估超过用户设定阈值时，触发用户确认（通过前端 API）
  - 支持按任务、按 Agent、按阶段的细粒度成本追踪
- 任务统计
  - 活跃任务数
  - 完成任务数
  - 失败任务数
  - 平均执行时间
- Agent 统计
  - 各状态 Agent 数量
  - 各角色 Agent 数量
  - Agent 利用率
- 健康检查
  - 系统健康状态
  - 各组件状态
  - 错误日志查看

#### Phase 7.7: 配置管理 API
- 全局配置
  - 获取系统配置
  - 更新系统配置（需权限）
  - 配置验证
- 工具掩码配置
  - 获取全局工具权限模板
  - 创建/更新工具权限模板
  - 应用模板到 Agent
  - 批量配置工具权限
- 模型配置
  - 获取可用模型列表
  - 配置模型参数（温度、top_p、max_tokens）
  - 模型成本配置

#### Phase 7.8: 多端协同与权限 API
- 多端协同
  - 允许前端以"只读身份"挂载（仅监控）
  - 允许前端以"管理员身份"挂载（可审批求助、修改配置）
  - 连接会话管理
  - 在线用户列表
- 权限管理
  - API Key 管理（创建、吊销、查看）
  - 权限级别定义
  - 操作日志记录

#### Phase 7.9: API 文档与基础设施
- API 文档
  - OpenAPI/Swagger 规范文档
  - 交互式 API 文档（Swagger UI）
  - TypeScript 类型定义生成（供前端使用）
- 基础设施
  - API 请求/响应日志
  - 请求限流
  - CORS 配置
  - 错误处理标准化
  - API 版本管理

---

### Phase 8: Flutter 前端 (MineClaw Control Center) 🎨
- [详细文档](./PHASE8_planning.md) (待编写)
- **设计理念**：保持轻量，专注核心功能，拒绝功能堆砌
- **目标**：提供直观的用户界面
- **API 依赖**：Phase 7 已提供核心 API 支撑

#### 核心功能范围
1. **对话界面**
   - 简洁的聊天界面
   - 支持 Markdown 渲染
   - 代码高亮显示
   - 消息历史记录
2. **可视化配置**
   - Agent 配置管理
   - 模型参数调整
   - 工具掩码设置
   - 工作流配置
3. **状态仪表板**
   - Agent 状态实时展示
   - 任务进度追踪
   - 系统资源监控
   - 错误日志查看
4. **像素工作室集成 (Pixel Studio) 🎮**
   - **全景工作流角色化**：Master Orchestrator 映射为"老板"，子总控映射为"主管"，普通 Agent 映射为"程序员"或"审查员"。
   - 状态同步：角色动作与系统状态实时关联
     - 运行任务时 → 员工疯狂敲键盘
     - 等待响应时 → 角色头上冒思考气泡
     - 任务成功时 → 角色跳舞或庆祝
     - 出错时 → 角色抓狂或垂头丧气
   - 互动元素：点击特定角色获取基于当前上下文的实时吐槽 (Bark)。
   - 成就系统：完成任务解锁工作室装修和角色装饰。

#### 非功能范围（不包含，保持轻量）
- ❌ 复杂的项目管理
- ❌ 代码编辑器
- ❌ 文件浏览器（可通过终端或 IDE 的 ACP 集成完成）
- ❌ 社交功能

#### 设计原则
- 简洁直观，拒绝过度设计
- 性能优先，流畅响应
- 像素工作室作为"调味剂"，不喧宾夺主
- 与 ACP 协同互补，各有侧重
- **API 优先设计**：前端功能随后端 API 完善而逐步增强

---

### Phase 9: 经验学习与持续进化 📋 (最低优先级)
- [详细文档](./PHASE9_planning.md) (待编写)
- **目标**：让系统从经验中学习，持续提升能力
- **优先级说明**：此阶段为长期规划，需在核心功能稳定后再实施
- **核心功能**：
  - 执行历史记录和分析
  - 成功模式提取
  - 策略优化
  - OpenViking 集成（上下文数据库）
  - EvoMap 集成（AI 自我进化）

---

## 核心特性路线图

### ✅ 已完成功能
- 基础对话 API
- 多轮对话
- MCP 工具调用
- SSE 流式输出
- 文件读写工具集
- Checkpoint 机制
- API Key 加密
- 配置动态修改
- 终端工具

#### Phase 4: 多 Agent 基础架构 (进行中)
- Agent 基础定义与生命周期
- 总控机制（支持嵌套子总控）
- Checkpoint 与会话增强
- 工具掩码基础机制
- 上下文管理 Agent（基础版）

#### Phase 5: 任务编排与路由系统
- 路由模型实现
- 工单机制实现
- 层级化总控架构
- 集群并行模式实现
- 接力转交模式实现
- 混合模式支持

#### Phase 6: 高级协作与质量保障
- 转交预热完善
- 消息追踪与审计
- 上下文裁剪增强
- 裁剪质量评分系统
- 提示词模板系统
- 线性流水线保障

#### Phase 7: 完整 API 层 (为前端铺路)
- 核心对话与 Session API + SSE 实时状态推送
- 多 Agent 与总控管理 API
- Agent 深度观察 (Inspector) API
- 任务与工单管理 API
- Checkpoint 与 AgentFS API（含 Diff 对比）
- 系统监控与统计 API
- 配置管理 API（工具掩码、模型参数）
- 多端协同与权限 API
- **API 完成后 Flutter 前端即可开始开发**

#### Phase 8: Flutter 前端 (MineClaw Control Center)
- 全景工作流仪表盘（动态展示 Agent 树与工单流转）
- Checkpoint 时间轴与代码变更对比 (Diff)
- CMA 告警与人工干预处理中心
- 对话界面与像素工作室角色互动系统
- 可视化配置与工具权限管理

#### Phase 9: 经验学习与持续进化 (最低优先级)
- 经验学习系统
- OpenViking 集成（上下文数据库）
- EvoMap 集成（AI 自我进化）
- **长期规划，核心功能稳定后实施**

### 🎯 长期规划
- 状态监控 API
- 沙箱安全
- 工作区规则自动提取

---

## 技术栈

### 后端
- Rust (主要语言)
- Axum (Web 框架)
- Tokio (异步运行时)
- agentfs (Checkpoint 管理)
- agentsql (数据持久化)
- OpenViking (上下文数据库)
- EvoMap (AI 自我进化协议)
- tokio-sync (并发原语)
- serde (序列化)

### 前端
- Flutter (跨平台 UI)
- Provider (状态管理)

### AI/ML
- OpenAI API 兼容接口
- Ollama (本地模型)
- OpenViking (上下文管理)
- EvoMap (能力进化)

---

## 关键设计决策回顾

### 1. 混合协作模式
- 结合集群并行和接力转交两种模式的优势
- 一个项目可以灵活组合使用两种模式

### 2. 层级化总控 + 可嵌套子总控
- 主总控 + 子总控的多层架构
- 子总控可以嵌套，知道自己的嵌套深度 (Level N)
- 提示词软性劝阻过深嵌套，但不硬性限制（保持灵活性）
- 子总控完成任务后写工单交回给父总控
- 总控只在关键节点介入，避免过度干预
- 总控也可以被重置，避免单点故障
- **因为模型强，所以敢给总控这么大自由**（创建 Agent、写提示词、规划工作流）

### 3. 复杂任务的两种执行模式
- **嵌套子总控接力模式**：适用于强依赖顺序的复杂任务
- **平行子总控集群模式**：适用于可并行分解的复杂任务
- 主总控根据任务性质选择合适的模式

### 4. 上下文管理 Agent (CMA) 是"监军"
- 独立监控所有 Agent，不直接执行
- **只有两个触发点**：Agent 主动求助、上下文满载
- 收到求助后决定回退和转交，通知总控执行
- 上下文满载时清理，并判断是否在持续犯错
- **定位是注意力管理，不是容量管理**：裁剪无关上下文提升质量，而非防止超限
- **CMA 上下文阈值自适应**：成功处理后放宽阈值，频繁失败则收紧

### 5. Agent 可以主动求助
- 普通 Agent 发现持续犯错时，生成求助工单发给 CMA
- 求助工单包含：犯错情况、建议回退的 Checkpoint
- CMA 而不是主总控接收求助

### 6. 工单机制简洁聚焦
- 只包含必要信息：已完成、文件路径、下一步计划
- 避免成为新的上下文负担
- **转交通过总控进行**：Agent A → 总控 → 总控 spawn Agent B
- **语义复述确认**：新 Agent 复述任务理解，总控确认后接管

### 7. 工具掩码精细控制
- 按单个工具粒度掩码
- 终端工具全开放
- 总控负责为 Agent 分配合适的工具

### 8. Checkpoint 与会话强关联
- 简化管理，避免孤立 Checkpoint
- 由 AgentFS 提供支持，轻量高效
- CMA 回退时使用 Checkpoint
- **按 Agent 分组的 Checkpoint 列表**：每个 Agent 维护自己的 Checkpoint

### 9. 转交预热必须
- 确保平滑过渡
- 避免信息丢失

### 10. 线性流水线
- 简化协作逻辑
- 避免复杂的Agent间依赖
- 完整的消息 ID 追踪
- **避免同文件冲突**：单向流水线 + 接口约定，不会出现同文件多 Agent 编辑

### 11. 裁剪质量可评估
- 为未来的自动优化积累数据
- 支持人工和自动评审
- **关键内容识别**：裁剪前标记"绝对不能删"的内容

### 12. 为经验学习预留接口
- Phase 6 就开始为 Phase 7 准备数据结构
- 确保平滑过渡到持续进化阶段
- **CMA 决策记录**：为 Phase 9 的 EvoMap (evomap.ai) 集成积累数据

### 13. 独立控制台优先 (取代 ACP)
- 放弃 ACP 的原因在于其"IDE 主导"的哲学与 MineClaw "后端自治"的 Checkpoint/AgentFS 机制冲突。
- 采用独立大屏前端可以实现对复杂多 Agent 协作和工单流转的完美可视化。
- 提供 API-First 的设计，通过 SSE/WebSocket 保障前端对底层状态的实时掌控。
- **先做前端再测试**：Phase 4-6 完成后优先开发 Flutter 前端，通过可视化辅助测试

### 14. 成本透明化
- **实时成本预估 + 阈值告警**：从"事后统计"升级到"实时预估"
- 超阈值触发用户确认（通过前端 API）

---

## 核心设计共识（2026 年时间节点）

在深入讨论后，我们明确以下设计共识：

| # | 共识点 | 说明 |
|---|--------|------|
| 1 | **CMA 定位是注意力管理** | 即使有 200k+ 上下文，保持高信噪比依然重要 |
| 2 | **API 便宜使能多 Agent** | 低成本 token 让我们可以负担多个 Agent 并行探索 |
| 3 | **模型强才敢给总控自由** | 信任总控去创建 Agent、写提示词、规划工作流 |
| 4 | **EvoMap 是已有项目** | evomap.ai 是真实基础设施，Phase 9 是集成 |

---

## 常见问题解答

| 问题 | 回答 |
|------|------|
| **编排延迟叠加会不会影响体验？** | MineClaw 主要针对后台/自动模式设计，而非实时交互。 |
| **工单转交会不会导致信息损失？** | OpenViking 作为上下文数据库会完整保留信息。 |
| **纯后端集成测试会不会很困难？** | 计划在 Phase 4-6 完成后优先开发 Flutter 前端，通过可视化辅助测试。 |

---

## 开发原则

1. **Baby Steps™**: 每次只做一个小的、有意义的改变
2. **The Process is the Product**: 过程和结果同样重要
3. **Incremental Validation**: 每一步都要验证
4. **Documentation First**: 先写文档再实现
5. **Concurrency Safety**: 并发安全是首要考虑
6. **Fault Tolerance**: 设计要考虑容错和恢复
7. **线性流程**: 保持流水线线性，简化协作
8. **可观测性**: 所有关键操作都要可追踪、可审计

---

## 相关项目

- **OpenViking**: AI 智能体的上下文数据库，采用文件系统范式统一管理记忆、资源和技能
- **EvoMap**: AI 自我进化基础设施，通过 GEP 协议实现 agent 间能力的共享、验证和继承

---

## 项目地址

[https://github.com/Cryptocho/mineclaw](https://github.com/Cryptocho/mineclaw)
